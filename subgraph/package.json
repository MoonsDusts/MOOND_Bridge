{
  "name": "moond-bridge",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sunlightluck/moond-bridge",
    "create-local": "graph create --node http://localhost:8020/ sunlightluck/moond-bridge",
    "remove-local": "graph remove --node http://localhost:8020/ sunlightluck/moond-bridge",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sunlightluck/moond-bridge",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
